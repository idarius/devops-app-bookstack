# Patch Kustomize pour l'URL publique de Bookstack en dev
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookstack
spec:
  template:
    spec:
      containers:
        - name: bookstack
          env:
            - name: APP_URL
              value: "https://bookstackdev.rncp.idarius.net"
