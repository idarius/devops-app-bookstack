apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookstack
spec:
  template:
    spec:
      containers:
        - name: bookstack
          env:
            - name: APP_URL
              value: "https://bookstackprod.rncp.idarius.net"
            - name: TRUSTED_PROXIES
              value: "*"